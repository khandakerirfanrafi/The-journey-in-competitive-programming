#include <bits/stdc++.h>
using namespace std;

int main()
{

    int n, q;
    cin >> n >> q;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
    sort(v.begin(), v.end());
    for (int i = 0; i < q; i++)
    {

        int x, l = 0, r = n - 1;
        cin >> x;
        while (l <= r)
        {
            int mid = (l + r) / 2;
            if (v[mid] == x)
            {
                cout << "found" << endl;
                break;
            }
            else if (v[mid] > x)
            {
                r = mid - 1;
            }
            else
            {
                l = mid + 1;
            }
        }
        if (l>r)
        {
            cout << "not found" << endl;
        }
    }

    return 0;
}
//problem link:[https://codeforces.com/group/MWSDmqGsZm/contest/219774/problem/Z]
